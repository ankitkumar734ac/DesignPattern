package com.host.bridge;

//Implementations
public interface Engine {
	void startEngine();

	void stopEngine();
}
/**
 * Implementations:
 * The Engine interface defines the operations that an engine should have, such as startEngine() and stopEngine().
 * The PetrolEngine and DieselEngine classes are concrete implementations of the Engine interface.
 * Each implementation provides its own implementation of the engine operations.
 */
